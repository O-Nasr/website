{
  "name": "study-docs-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "convert": "node scripts/convert-docx.mjs",
    "dev": "node scripts/convert-docx.mjs && vite",
    "build": "node scripts/convert-docx.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "mammoth": "^1.12.0"
  }
}
